import { MyorderDirective } from './myorder.directive';

describe('MyorderDirective', () => {
  it('should create an instance', () => {
    const directive = new MyorderDirective();
    expect(directive).toBeTruthy();
  });
});
